# Sentry error reporting. Leave empty to run with error reporting disabled
# (the SDK is only enabled in production builds; see src/instrumentation*.ts).
NEXT_PUBLIC_SENTRY_DSN=

# Only needed when uploading source maps from CI.
SENTRY_AUTH_TOKEN=
